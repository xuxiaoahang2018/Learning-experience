package main

import (
    "fmt"
)

const (
    i = iota
    j = iota
    k
    l
)

func main() {
    fmt.Println("i=", i)
    fmt.Println("j=", j)
    fmt.Println("k=", k)
    fmt.Println("l=", l)
}
复制代码
 

输出结果：

i= 0

j= 1

k= 2

l= 3

iota是一个比较奇怪的存在，它只能用在const的申明中，是一个从0开始的行数索引器
